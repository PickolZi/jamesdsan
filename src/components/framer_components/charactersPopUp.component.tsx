import { motion } from "framer-motion";


type Props = {
    text:string
}

const CharactersPopUp = ({text}:Props) => {
    return (
        <span>Character pop up!</span>
    )
}

export default CharactersPopUp;